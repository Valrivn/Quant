# D-20260807-002 — Instagram anti-bot scraper build (real IG feed)

- **Tier:** DISCOVERY (straight to CEO)
- **Date:** 2026-08-07
- **Brief:** B-20260807-002 (in-session plan approved by CEO)
- **Ruling:** APPROVE

## Options considered
1. big-pickle plan (in-session): reuse Reddit/Glassdoor anti-bot toolkit (nodriver CDP stealth, SeleniumBase UC, curl_cffi JA4, proxy rotation, circuit breaker, rate limiter, metrics) adapted to Instagram's stricter auth/challenge model.
2. Alternative A: install instagrapi/instaloader third-party IG libs.
3. Alternative B: public-only scraping, no logged-in session.

## Positions
- big-pickle: own the anti-bot via existing toolkit (nodriver primary, curl_cffi fallback), no new heavy deps.
- gemini-planner: not invoked (Discovery tier, straight to CEO per constitution).

## CEO ruling
> APPROVE (in-session, all four plan decisions confirmed):
> 1. Auth = cookie import from CEO's logged-in browser to `config/instagram_cookies.json` (git-ignored); no credentials in repo.
> 2. Results scope = real IG mentions into the discovery feed -> standard screen -> alpha backtest; qual-gate pass-through remains a separate unblock (sentiment sources DEGRADED).
> 3. Config = additive `instagram` source weight in `config/weights_discovery.yaml` (re-normalized) + recorded as a config-change decision.
> 4. Engine = nodriver CDP stealth primary; curl_cffi + cookies + proxy fallback.

**Rationale:** CEO chose all four recommended options. IG is an INDEPENDENT experimental channel (per D-20260807-001); this build makes the real feed exist so the experiment can produce real candidates.

## Effect
- Blueprint delta: IG independent feed gains a real anti-bot scraper (cookie-session InstagramSession); discovery feed remains leaf; kill-switch `discovery.enabled: false` stays; `discovery/` remains leaf.
- Quality gate: PASS (pending conductor gate after build).
- Cost rows: D-20260807-002 -> cost-ledger.md

## Follow-up
- deepseek-worker built `Qualitative/psychological/scrapers/instagram_primary.py` + discovery wiring + config deltas + offline tests (42 pass).
- Live run requires CEO to drop a real session cookie file into `config/instagram_cookies.json` (git-ignored).
- Record in decisions/index, cost ledger, tendencies, blueprint.
- No commit without CEO approval.
