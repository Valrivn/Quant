POSITION-BY: gemini-planner (FALLBACK: big-pickle wrote the adversarial side —
  degradation ladder Step 2, gemini-planner subagent returned 3 empty runs)
BRIEF-ID: B-20260803-001
POSITION: REJECT as stated — run a pre-registered falsification test on the
existing 1002-name universe BEFORE funding the insider-buying datastore; the
thesis as written repeats the P3 failure mode (selectivity on a single factor
family, gated on unproven data).
REASONS:
- The cohort evidence does NOT support the premise: HIGH_REINVEST matched
  PROFITABLE on mean (53% vs 51% 3y) but lost on win rate (57% vs 76%), and
  OTHER (neither factor) had the HIGHEST mean (127%) — a loud warning that
  single-family gates on this universe overfit right-tail outliers. Adding
  "profit" back on top of a factor that already fails selectivity is not
  evidence-tested.
- Every new factor here needs its own PIT datastore: rising gross margin
  (not yet in the XBRL field map — we surface operating margin, not gross),
  insider buying (new feed entirely), multi-year lookback windowing. Each is a
  survivorship/PIT-leak risk and a maintenance cost. The blueprint forbids
  funding data before falsification (D-20260802-FINAL: FALSIFICATION FIRST).
- "MOST factors close enough" is under-specified: what threshold, how many of
  four, and how "close" is measured? Undefined, it is a backtest dial that will
  be turned until the result looks good — exactly the D-20260801-004 audit
  finding (selection on the same window, deflated CIs).
RISKS:
- Insider-buying is small-sample, noisy, and easily PIT-corrupted (form 4
  filing lag, clustered buys); as a gate it can silently drop the whole thesis
  to a handful of names — no capacity.
- The watchlist ("companies close to achieving these things") is not an
  investment rule; as a signal it creates lookahead if it leaks knowledge of
  names that later pass.
BLIND-SPOTS: I did not re-run the cohort splits per factor (gross margin /
cashflow trend / insider) — the counter-hypothesis is unmeasured; I did not
verify whether gross margin data exists in the XBRL field map today.
