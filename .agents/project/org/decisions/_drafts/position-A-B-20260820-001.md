# Position A — big-pickle (B-20260820-001)

STANCE: BUILD — with two of B's constraints adopted. The design is optimal *as
a discovery-generation lane*; it was never proposed as a backtest source.

1. B's PiT attack aims at the wrong layer. The funnel generates candidates; it
   doesn't simulate history. IG was demoted for noise, not for being
   current-state. Every wiki candidate still faces CIK validation + the full
   G1–G4 funnel + quant baseline. Adopt B's firewall explicitly: wiki lane is
   live-screener only, firewalled from backtest-agent. With that wall the
   fatal flaw dissolves — no look-ahead can enter a system that never looks
   backward through this lane.
2. DFS noise trap: half-concede. Without LLM parsing, topic descents WILL pull
   junk. But the defense is structural, not semantic: descents trigger only on
   topics adjacent to ≥K CIK-validated high-grade companies, and every
   extracted entity passes ticker+class+CIK filters before counting. The
   ≥10-descent experiment IS the falsification gate — if the false-positive
   rate mimics IG's 315-junk floor, the lane dies at P1 having cost almost
   nothing.
3. Small-cap sparsity is a measurement, not an assumption — that is precisely
   what the wiki-only diff bucket quantifies. Free APIs, zero credentials,
   own tables, frozen cores untouched: downside bounded to P1 evidence;
   upside is the densest structured entity graph in existence.
4. USPTO/USASpending is complementary, not substitutive. Patents measure R&D
   output; contracts measure government sales; neither gives ownership/supply
   breadth. Both belong in a DISCOVERY-tier catalog entry later — good catch,
   wrong replacement.
5. Cost: ~35k tok, offline-fixture tests, separate APPROVE ruling before any
   wiring.

VERDICT: APPROVE with amendments — (a) hard backtest firewall, (b) DFS exists
only behind the pre-registered experiment.
