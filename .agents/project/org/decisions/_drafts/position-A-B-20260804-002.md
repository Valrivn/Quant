POSITION-BY: big-pickle
BRIEF-ID: B-20260804-002
POSITION: Approve the full PIT data-accuracy rebuild as written — 7-phase data-layer-only build (P1-P7), pre-registered S1-S6, then Discovery re-run reporting % deltas vs the degraded run with zero strategy-logic changes.
REASONS:
- Verified in code: static DIVIDEND_YIELDS (sleeves.py:52) accrues shares*p*y/252 daily (fee_sim3.py:294) — a 2026-dated yield map cannot truthfully price 2018-2026 dividend income; dual raw/adjusted series with real ex-date events (P2) is the only honest execution math, and it kills the silent same-day-fill look-ahead too.
- Verified: _nasdaq_crosscheck (fee_sim3.py:677) runs in main() only, not run_sim_discovery() (fee_sim3.py:823); routing it through a shared validate_prices() gate hit by EVERY runner plus the S1 DEGRADED ledger makes fallbacks visible, satisfying the Provenance-everywhere invariant the current silent NA/price-proxy path violates.
- Build order is right: P1-P6 fix inputs, P7 re-runs identical strategy code, so any % delta is attributable to data alone; S5 then honestly answers whether B-20260804-001's directional read (incl. 72/102 bear) survives, and the auditor no-hardcoding gate keeps the re-run from being gamed into a pass.
RISKS:
- New API surfaces (fredapi/ALFRED, edgartools, Tiingo/Stooq/OpenBB) carry key/rate/blocking risk; pinned parquet cache + checksums mitigate but may stretch schedule.
- A large P7 delta creates pressure to "correct" results; only the pre-registered success bars + conductor gate hold the line.
BLIND-SPOTS: Did not verify today's FRED/Tiingo key availability or quotas; did not run edgartools/fredapi locally; did not audit full run_sim_discovery internals beyond locating the crosscheck gap; did not test sqlite/parquet cache on Windows path-length limits.
