BRIEF-ID: B-20260804-002
TIER: 3
TASK: Rebuild the backtest data layer to point-in-time accuracy and re-run the Discovery comparison (B-20260804-001) on corrected data, quantified as a delta vs the degraded run.
CONTEXT:
- Discovery B-20260804-001 ran with 7 documented data degradations (FRED + EDGAR down, no Nasdaq cross-check on the discovery path, empty 2018-2020 buy-more basket, static dividend yields, rate-limit, stale baseline) - results are directionally suggestive, not fully accurate.
- 2026 industry evidence: corporate-action dual-series handling, PIT filing-date keys (IC 5.38% naive vs 2.88% PIT), survivorship-free universes, and fill-at-next-open separate honest from fake backtests.
- CEO ruled TIER 3 - data integrity underpins every future backtest and the T3 promotion decision for the return-max pivot.
STAKE: high - a wrong backtest could greenlight a losing strategy or kill a winning one; this rebuild changes how ALL results are produced.
CONSTRAINT: invariant-4 pre-registration (config/weights_diversification.yaml) unchanged; NO strategy-logic changes - data layer only; pinned caches for reproducibility; all six success criteria pre-registered; auditor OOS/no-hardcoding discipline.
BLUEPRINT-EXCERPT: "Architecture invariants (T3-grade): 2. **Provenance everywhere.** Every fused score carries source provenance; audit module can trace any number to its inputs. 5. **Anything decision-critical ships with tests.** D-20260803-003: (5) auditor gate: NO hardcoded weights/picks/params in the backtest, OOS validation mandatory; (6) multi-source data (FRED/SEC EDGAR etc., not only yfinance)."
SUCCESS:
- S1 Zero silent degradations: every source live-verified or explicitly DEGRADED-tagged in the run registry on every run - never a silent fallback.
- S2 Two+ independent price feeds for all traded sleeves; daily-return corr >= 0.99 + raw-level reconciliation; AAPL-split and dividend ex-date integrity tests pass.
- S3 PIT discipline: every macro/fundamental input carries an availability date; no-future-information audit tests pass; decisions fill at next open, not same close.
- S4 Survivor-free universe + real ex-date dividend events (static yield map removed); buy-more basket populated from 2018 - no empty-buy-more.
- S5 Discovery re-run: all 8 strategies on corrected data; % delta vs degraded numbers reported; success bars re-evaluated honestly.
- S6 Conductor gate >= 90%, zero new failures, baseline-test-results.json refreshed.
