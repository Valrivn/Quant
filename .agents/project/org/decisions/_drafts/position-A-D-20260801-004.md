POSITION-BY: big-pickle
BRIEF-ID: D-20260801-004

1. INTERPRETATION — The data shows one thing: the "edge" is a 10-name AI-capex theme bet wearing an alpha costume. EDGE_REAL is an artifact of the ablation design: A>B only proves that dropping megacaps halves mean alpha (0.053→0.034) and collapses share-positive to 50% — the bias IS the edge. Top-5 (NVDA/SMCI/STX/AVGO/MU) are AI hardware/storage; NVDA's residual alpha is FF5 failing to price the AI factor, not skill. Laggards are the mirror: ADBE (AI-substitution), SWKS (handset RF missed AI), HPQ/UNH (no AI exposure). 56%/50% share-positive ≈ coin flip; the mean is right-tail drag from NVDA. L3 OOS alpha CI includes zero; only gold's 10y sleeve alpha is significant — a known regime bet, not discovery.

2. AUDIT — Statistically unsound in five places: (a) selection on the same window — top-15 pool and baskets are chosen and scored in-sample; k5/k10/k15 are nested, not independent; CIs ignore selection (Bonferroni for 50 names → α≈0.001; SMCI/STX CIs already include zero). (b) daily residual SEs need Newey-West/HAC; naive OLS CIs understate. (c) deflated Sharpe 1.00 = zero margin — barely-survived boundary; with 6 configs tried, deflation is weak. (d) short_bills sleeve −2.05% return, Sharpe −2.04 is a data bug — bills cannot lose money; it poisons any L2/L3 result. (e) structural gaps: excess_1y=excess_3y (horizon slicing broken, pre-slippage returns), FRED gate inert so tested ≠ designed system, sleeve excess NaN, L3 decisions not persisted. Slippage 0.5% must be per-rebalance, not daily, or it dwarfs all alpha.

3. RISKS + RECOMMENDATION — Do NOT trade k5/k10/k15: one factor bucket, correlated names, thin capacity, no true OOS, vulnerable to AI re-rating. Keep megacaps only as an explicit, documented, hedged style tilt — not discovered alpha. Required before commit: real walk-forward L1 (rank half-1, score half-2); fix horizon/excess/slippage consistency; fix short_bills; restore or formally disable regime gate; selection-adjusted CIs + HAC SEs; persist decisions.

BLIND-SPOTS: Did not inspect results.json, rebalance-cost math, or slippage unit (daily vs per-rebalance).
