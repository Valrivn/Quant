# Disagreement Map — B-20260820-001 (big-pickle, ≤200 tokens)

1. BUILD vs REPLACE: A builds wiki lane now (USPTO later, complementary);
   B replaces it with USPTO/USASpending primary (native timestamps), wiki
   demoted to bulk-pull-only if mandated at all.
2. DFS TOPIC DESCENT: A keeps it, gated behind the ≥10-descent falsification
   experiment; B scraps it entirely — deterministic mapping of noisy topic
   neighborhoods without LLM parsing recreates the IG junk floor.
3. CONVERGED (not splits): hard firewall wiki↔backtest-agent (live screener
   only); hub/class defenses; research-only governance with separate APPROVE
   ruling; diff harness as P1 evidence; free-API/no-credential constraint;
   SEC as sole truth anchor.
