SYNTHESIS-ID: S-20260820-001
RECOMMENDATION: hybrid — build wiki SPARQL BFS lane NOW (structured edges P355/P127/P749 only); DFS topic descent GATED behind pre-registered ≥10-descent falsification experiment that must clear before any merge; hard backtest firewall enforced from day one; USPTO/USASpending catalogued as complementary, not primary replacement.

DISAGREEMENTS:
- SUBSTANTIVE: DFS fate. A keeps DFS behind a falsification gate; B scraps it entirely. Gate is the correct resolution: it forces empirical death if false-positive rate mirrors IG's 315-junk floor.
- SUBSTANTIVE: Build-vs-replace. A builds wiki+USPTO later; B replaces with USPTO primary. Replacement is premature — USPTO answers R&D output, not ownership topology; the supply-chain gap wiki addresses is real.
- STYLISTIC: Wikidata coverage sparsity concern — both sides measure it via diff harness; not a blocking disagreement.

RATIONALE: The PiT attack (B's fatal-flaw #1) is architecturally neutralized by the hard firewall: a lane that never feeds backtest-agent cannot inject look-ahead bias. Structured SPARQL edges (P355/P127/P749) are deterministically CIK-mappable, low noise, and free — the narrow, defensible subset B itself endorses. DFS is high-variance but falsifiable at near-zero cost in P1; killing it pre-experiment discards real optionality.

ARCHETYPE COUNCIL:
- sim-guardian: Firewall is the sole PiT control; it must be wired before any wiki entity touches backtest-agent — structural, not procedural.
- risk-automator: Downside bounded to ~35k tokens and P1 experiment time if DFS dies; acceptable.
- execution-strategist: Free APIs, no credentials, offline fixtures — fill/data discipline intact.
- alpha-integrator: Supply-chain edge sparsity at small-cap is the capacity ceiling; diff harness quantifies it in P1 before signal sizing.
- devil-advocate: Weakest claim in A: "DFS noise is gated by CIK adjacency" — adjacency filter is unproven and may not shrink the false-positive pool enough without LLM parsing. Weakest claim in B: USPTO "natively solves PiT" — patent grant dates lag filing by years; timestamping is cleaner but not clean.
- open-minded (confidence 72% hybrid): What would flip it to full B → if ≥10-descent experiment returns false-positive rate ≥ IG's 315-junk baseline, abandon DFS and pivot USPTO primary immediately.

RISK: DFS adjacency filter is untested; if it fails the falsification gate the lane reverts to bulk SPARQL only — acceptable, not catastrophic.

DECISION-REQUESTED: CEO: approve hybrid (SPARQL BFS lane live + DFS behind pre-registered falsification experiment, hard backtest firewall, USPTO/USASpending catalogued as future complement), or reject and mandate USPTO primary replacement?
