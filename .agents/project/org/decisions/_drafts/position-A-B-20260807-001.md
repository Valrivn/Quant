POSITION-BY: big-pickle
BRIEF-ID: B-20260807-001
POSITION: Keep IG/TikTok as a corroborating peer gated on the same structured-source evidence — do NOT decouple it as an INDEPENDENT channel — and re-sequence the multi-phase trajectory to populate the credential files first, because the census proves 0% pass-through is a credentials gap, not an architecture gap.
REASONS:
1. Census P1: SEC EDGAR ran LIVE (500 mentions, 500 validated) yet 0/20 passed the qualitative gate — every gated ticker was `avoid` because sentiment/fundamental signals are DEGRADED; an independent IG channel adds no evidence the gate can use.
2. Reddit, StockTwits, ApeWisdom are all DEGRADED on empty/placeholder credentials (client_id/secret empty, STOCKTWITS_ACCESS_TOKEN env unset, APEWISDOM_API_KEY missing); the cheapest unblock is filling three config files, not building a new channel.
3. Video gate is LOCKED (`VideoSourceLockedError`, no candidate pass), so decoupling now breaks D-20260806-001's falsification-first fail-closed contract and the discovery/ isolation invariant.
RISKS:
1. An independent video channel with zero live evidence invites mission drift and unprovenable alpha; clout-chaser/ad-spam noise cannot be provenance-gated.
2. The multi-phase path could stall indefinitely if credentials never arrive; keep P1–P5 fail-closed and refuse scope expansion while pass-through is 0%.
BLIND-SPOTS: Did not verify real credential availability (Reddit script-app quota, StockTwits/ApeWisdom free keys), IG/TikTok scraping feasibility/ToS, census 20-ticker cap effects on pass rate, or Glassdoor tilt behavior while DEGRADED.
