# Position A — big-pickle (Manager A) for Brief B-20260807-002

**Recommended:** Hybrid live/cached with strict provenance + honest neutral default.

## D1 (quant gate): batch across run; live-first, cached-fallback, provenance-tagged
Fix the batching bug so a run evaluates its whole candidate set in ONE frame
(`alpha_z` needs ≥2 non-NaN alphas; `quant_baseline_flags` computes z
within-frame by design). Attach real per-ticker data:
- `alpha_3y_ann`: compute live (yfinance + Ken French, same as the research
  doc's own per-name backtests) for every candidate with price history. Where
  live fetch fails, fall back to the cached `results.json` run_a value IF the
  ticker is in the roster. Row is tagged `source: live_ff5` or
  `source: results_runa_cached`. NaN when neither (honest fail, never invented).
- `cash_burn_months_pct` / `interest_coverage_ratio_pct`: live SEC XBRL
  `fetch_companyfacts` per candidate (soft timeout). NaN when unavailable — the
  screen already treats NaN as a pass on those legs, which is correct: no data
  is not evidence of insolvency.

## D2 (qualitative gate): fix the empty-input defect to neutral "hold"; wire cached signals where they exist
Two independent parts:
1. **Honesty fix:** with zero moat signals, `run()` yields `ema_60d=0.0` →
   blended 0.0 → "avoid". That is a *wiring* artifact, not evidence of
   avoidance; the docstring already promises a neutral "hold". Default missing
   signals to neutral so the gate reports `qual:hold` (fail, honestly) instead
   of the misleading `qual:avoid`.
2. **Real-signal adapter (cached only):** map genuinely-cached DB data to moat
   keys for the ~10 megacap cohort names: `employee_sentiment` from
   `glassdoor_comparably_audit` (already normalized 0–1), `developer_momentum`
   from `github_org_metrics`, `product_breadth` from `product_intel_reviews`,
   `network_effect_proxy` / `regulatory_barrier` default 0.5 tagged
   `default_neutral`. This is real evidence, not fabrication. Names without
   cached signals get "hold" — they still fail, just honestly.

## D3 (mahalanobis): percentile-normalize within the batch to 0–1
Agree with normalizing (not relaxing the floor). The screen's floor
(>95th pct fails) is the documented intent; the engine outputs raw distance.
Normalize each batch's distances to a 0–1 percentile so the floor means what
it says. No magic constant.

## D4 (integrity): same gates both lanes; per-row provenance; documented bias
- Identical gate code for IG and traditional lanes (already true; keep it).
- Every names-frame row carries `source` provenance; the comparison report
  prints coverage counts (n live_ff5 / n cached / n default_neutral) so the
  CEO can see the vintage mix, not a false impression of uniformity.
- Never fabricate: NaN/None when unknown; no invented signals or alpha.
- Coverage bias is documented: qual signals exist only for the megacap
  cohort; the report says so explicitly.

## Strongest disagreements vs Position B
1. **D2 "avoid" is a defect, not a policy.** B wants to keep fail-closed
   "avoid". But a missing-input "avoid" mislabels missing data as an avoid
   signal; it is a bug against the code's own docstring. "hold" cannot pass
   the gate, so B's "rewards data-poor assets" concern is moot — data-poor
   names still fail; they just fail honestly.
2. **D1 cached data is not cross-sectional corruption when tagged.** B rejects
   any cache on purity grounds. Provenance makes the vintage transparent and
   the report flags mixed rows. Rejecting cache outright turns the experiment
   into a network-availability lottery (one blip → whole lane `no_alpha_data`)
   and blocks a result today for no integrity gain the tags don't already buy.
3. **B's strictness yields a vacuous 0/0 comparison.** B's "zero-pass risk is
   acceptable" leaves D-20260807-001 unanswerable: both lanes empty, alpha
   None, nothing learned. A's hybrid at least lets the traditional lane's
   megacaps (which genuinely have signals) form a pass cohort, giving a
   non-empty like-for-like comparison this run.
