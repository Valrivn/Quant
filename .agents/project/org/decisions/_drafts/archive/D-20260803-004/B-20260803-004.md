BRIEF-ID: B-20260803-004
TIER: 3
TASK: Phase 2 of D-20260803-003 — build the opportunistic stock engine + dividend
engine with stable-dividend audit, and integrate both into the multi-asset $10k
sim (fee_sim3) as a new strategy variant.
CONTEXT:
- Both Phase-2 greenlight conditions are MET: Phase 1 passed conductor
  (857/18/0, 97.9%) and dividend-fee-coverage was recorded (53-71x, registry).
- Fee-aware liquidate-only rule (D-20260803-002) is the floor: sell only to
  redeploy or de-risk; no rebalancing churn.
- Multi-source data is mandatory: stable-dividend inputs must be audited against
  a second source (SEC XBRL cash dividends + Nasdaq API / yfinance price
  yields), not yfinance-only.
STAKE: high — this spends a build round and integrates two engines into the
production sim; wrong scope repeats D-20260802-002's failed-pilot lesson.
CONSTRAINT: no hardcoded weights/picks/params (auditor gate); all thresholds
pre-registered; OOS purge+embargo mandatory; D-20260803-002 liquidate-only rule
preserved; dividend yields used for fee-coverage measurement only, never
selection.
BLUEPRINT-EXCERPT: "PHASE 2 (deferred, separate brief): opportunistic stock
engine + dividend engine + stable-dividend audit — greenlit only after Phase 1
conductor pass + dividend-fee-coverage recorded. D-20260803-002 fee-aware
liquidate-only rule preserved and applies to new sleeves."
SUCCESS:
1. Opportunistic engine + dividend engine (stable-dividend audit) live in
   diversification/.
2. Dividend screen inputs from >=2 sources (SEC XBRL + yfinance/Nasdaq price
   yields).
3. Both engines integrated into fee_sim3 as a new strategy variant; $10k sim
   re-run vs BASELINE + MACRO + MINVAR.
4. Auditor certifies no hardcoding; OOS shown; coverage re-measured.
5. Conductor >=90%, 0 new failures.
