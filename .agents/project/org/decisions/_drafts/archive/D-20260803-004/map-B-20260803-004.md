DISAGREE-ON:
- Opportunistic engine: A = reuse D-20260803-002 absolute-swap/liquidate-only rule
  as-is, equity sleeve only; B = fresh multi-asset definition tied to the bear
  regime, large-caps only, and deferred to a later validation step.
- Stable-dividend audit window: A = >=10y continuous payout, no >50% y/y cut, no
  skipped year, >=3% yield floor, REIT/BDC/MLP excluded; B = 5y unbroken payout,
  Nasdaq yield floor, special dividends excluded.
- Integration: A = new DIVIDEND strategy variant in fee_sim3 (BASELINE/MACRO/
  MINVAR untouched); B = cash-sweep overlay, not a distinct variant, to isolate
  coverage vs the exact Phase-1 baseline.
- Sequencing: A = four-step build (audit module -> opportunistic reuse -> DIVIDEND
  variant -> OOS/auditor/conductor); B = stable-dividend audit first as pure
  funding overlay, opportunistic deferred.
CONSENSUS-ON:
- Pre-registered thresholds, no hardcoding; multi-source dividend validation
  (SEC XBRL + Nasdaq/yfinance price yields); OOS purge+embargo; auditor +
  coverage re-measure + conductor gate; small-cap floor preserved.
RESOLUTION-PATH:
- Integration: overlay isolates coverage, variant keeps comparability — CEO picks.
- Audit window: 5y vs 10y must be feasible from the first decision date inside
  2018-2026 expanding windows (10y impossible before 2018+10y) — pre-register the
  shorter feasible window or start data pre-2018.
- Opportunistic: tie reuse/fresh to pre-registered bear-regime state, fee-aware
  gate binding — settles the reuse-vs-regime objection.
