SYNTHESIS-ID: S-20260803-005
RECOMMENDATION: hybrid — build min-variance GD as a pre-registered ablation only (not the default engine); keep 40/20/20/20 as the neutral MACRO_TARGETS["neutral"] anchor (not a strict constraint), preserving D-20260803-003 rotation; add profit-change as a pre-registered bear-state OR-gate flag alongside the z-gate, measured via fee-churn attribution before any adoption; all thresholds and the train/OOS split committed to config/weights_diversification.yaml before any run.

DISAGREEMENTS:
- SUBSTANTIVE: GD vs no-GD — outcome-material; resolved by ablation-first gate (GD adopted only if OOS beats fixed-mix under auditor discipline). Profit-change overlap with z-gate — material to attribution; resolved by OR-gate flag with separate churn reporting, never replacing the z-gate.
- STYLISTIC: "constraint bounds" vs "neutral anchor" framing of 40/20/20/20 — difference in language only; both positions preserve the target weights.

RATIONALE: Running GD as an ablation rather than the default satisfies B's overfitting concern while preserving A's min-variance optionality, with the auditor's OOS gate as the binding adoption criterion. Treating 40/20/20/20 as the neutral anchor keeps D-20260803-003's macro rotation intact, which a strict-constraint reading would silently delete. Profit-change as a flagged OR-gate resolves the attribution muddying B identified without discarding a potentially valid fundamental signal.

RISK: If the ablation GD result is presented before the pre-registered yaml is committed, the auditor's no-hardcoding rule fails and the entire comparison is invalid.

DECISION-REQUESTED: Does the CEO approve GD as an ablation (not default) with profit-change as a separate OR-gate flag, both gated by a pre-committed config/weights_diversification.yaml and auditor OOS discipline, before any build begins?
