POSITION-BY: big-pickle
BRIEF-ID: B-20260803-005
POSITION: APPROVE with modifications — keep MACRO_TARGETS state rotation as the anchor (40/20/20/20 equals sleeves.MACRO_TARGETS["neutral"], not a replacement, and the new four-sleeve decomposition must not silently drop short_bills/gold); add profit-change as a pre-registered bear-state OR-gate complement to the opportunistic z-gate (never a replacement); enter small/mid only via liquid ETF proxies (MDY/IWM) under the 10-15% floor; optimize min-variance with a fixed pre-registered train/OOS split and never maximize cash (yields stay measurement-only per D-20260803-004); make cash-shortfall relocation a pre-registered ablation — all weights/thresholds pre-registered in a new config/weights_diversification.yaml before any run (invariant 4).
REASONS:
- D-20260803-003 approved the macro rotation (bull->bonds, bear->stocks) already shipped in fee_sim3.target_macro; freezing one 40/20/20/20 mix deletes that ruling's asset, so keep rotation and treat 40/20/20/20 as the neutral anchor + fixed baseline.
- opportunistic.py fires on price z (OPPORTUNITY_Z=-1.0, 5y, bear-only, TILT_MULT=1.5, equity-only, liquidate-only); profit-change is a fundamental signal and complements as an OR-gate, but D-20260802-002 made the discovery gate reinvestment-rate/profit-agnostic, so it must not hard-exclude reinvesting small caps.
- Small/mid stock selection stays gated: D-20260803-001 falsification-first and D-20260803-002's 10-15% floor; only ETF proxies with pre-registered tickers in sleeves.py + config satisfy the universe question today.
RISKS:
- Fitted weights/hyperparameters can overfit the held-out segment; the split point and hyperparameter grid must sit in the pre-registered yaml before any run, else the auditor's OOS/no-hardcoding gate fails.
- Profit-change thresholds can silently drift into outcome tuning and re-litigate the reinvestment-rate thesis; every threshold must be a module/config constant, reported as an ablation.
BLIND-SPOTS: Did not verify tests/test_* coverage for these modules, MDY/IWM price-history availability in datastore.fetch_sleeve_prices back to 2015, or current pass rate; no config/weights*.yaml exists yet, so invariant-4 wiring is inferred from blueprint, not confirmed; did not read position-B.
