POSITION-BY: gemini-planner
BRIEF-ID: B-20260823-001
POSITION: Implement a strict "Time-Travel DB" sandbox wrapping SQLite with a mandated simulated-clock timestamp parameter for all reads, evaluating pipeline skill via cross-entropy loss against verified human corpora (PhraseBank/SemEval) before simulating any market returns.
REASONS: 
1. (PIT Mechanics) A simulated-clock wrapper on the data access layer physically prevents look-ahead bias without modifying core db/ storage files.
2. (Evaluation) Predicting established human labels separates pure NLP pipeline skill from noisy downstream alpha signals, addressing verified-vs-raw efficiently.
3. (Ablations & Protocol) Generating a strict combinatorial source ablation matrix with frozen weights prior to tuning isolates the true marginal value of each raw source.
RISKS: 
1. Time-travel query wrappers can introduce severe read latency if the underlying event logs aren't heavily indexed by timestamp.
2. High predictive accuracy on verified sentiment corpora (PhraseBank) might be orthogonal to actual financial returns, yielding a false positive on "skill".
BLIND-SPOTS: I assume the NLP pipeline outputs calibrated probability scores directly comparable to SemEval/PhraseBank labels; I have not verified the SQLite schema or index coverage to confirm if a clock-wrapper is performant.
