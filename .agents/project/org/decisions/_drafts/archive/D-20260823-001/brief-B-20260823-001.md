# BRIEF B-20260823-001 — STACK A SANDBOX: PROVE PIPELINE SENTIMENT SKILL

BRIEF-ID: B-20260823-001
TIER: 3
TASK: Design the optimal implementation plan for an isolated PIT sandbox backtest
(Stack A, facts-consistent 2009–2026) whose sole hypothesis under test is:
"the house qualitative/analyst pipeline adds value over mechanical baselines
when analyzing financial sentiment."

CONTEXT:
- CEO reframe: test targets PIPELINE SKILL, not source luck. Every feature must
  exist PIT-clean across the full window or it is excluded. No mid-window
  information-set changes.
- Sources tiered: VERIFIED sentiment (human-labeled corpora: Financial
  PhraseBank ~4.8k sentences, SemEval-2017 T5, Strux transcripts w/ forward-
  return labels 2017-24) vs RAW sentiment scored by OWN engine over raw text
  (idleengine transcripts 33k calls/685 cos 2005-25; SEC FSDS/XBRL 2009+;
  Damodaran archived industry tables incl. synthetic credit ratings; LM
  Master Dictionary 1993-2025 as scoring instrument; Chen-Zimmermann factors
  as controls). All free, no API keys.
- CEO requires: absolute isolated sandbox (no look-ahead); per-source ISOLATION
  ablations compared head-to-head; verified-vs-raw organization; tuning only
  AFTER baseline verdicts recorded. CEO prefers NO manual hand-grading — use
  human-labeled corpora as grading proxy where auto-validation is impossible.

STAKE: HIGH — this is the primary falsification instrument for the whole
qualitative pillar; a tainted run invalidates every future claim.

CONSTRAINT: blueprint invariants 1-6 (DB-first WAL, provenance everywhere,
weights in config/weights*.yaml pre-registered before any run, tests mandatory,
no silent fallbacks/data-status ledger on every run); falsification-first
(D-20260803-001); relative-vs-baseline bar (D-20260806-001); frozen cores
untouched (SHA-256 manifest); research-only until conductor gate + APPROVE.

BLUEPRINT-EXCERPT: "Anything decision-critical ships with tests... pass rate
target ≥90%... Weight changes go through config/weights*.yaml, not hard-coded
constants... all persistence through db/ layer." Quality-gate ≥90% pytest.

SUCCESS: A written plan specifying (1) sandbox architecture guaranteeing PIT
isolation, (2) source-isolation ablation matrix, (3) verified-vs-raw evaluation
design replacing manual grading, (4) pre-registered success bars + tuning
protocol, (5) build phases with conductor gates — such that if executed, a
PASS/FAIL verdict on "pipeline analyzes sentiment well" is defensible against
sim-guardian audit.
