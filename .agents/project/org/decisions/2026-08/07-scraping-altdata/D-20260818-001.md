# D-20260818-001 — Enable supervised live consensus (G2/Glassdoor/Indeed)

- **Tier:** 2
- **Date:** 2026-08-18
- **Brief:** B-20260818-001 (audit + impl-plan drafts)
- **Ruling:** APPROVE

## Options considered
1. Flip `consensus.enabled=true`, add `--live-consensus` to the active pipeline, wire supervisor health, verify on one live run (CEO picked).
2. Stage everything without flipping the kill-switch (offered, not picked).
3. Keep consensus off entirely (offered, not picked).

## CEO ruling
> "Run an audit as why it failed make an implementation plan to solve it." → later
> APPROVE (build it) on impl-plan B-20260818-001 via the decision prompt.

**Rationale:** consensus live pass was governed off (D-20260816-001 kill-switch) + hardwired
off in `scrape_cmd.py:131-132` (`DISCOVERY_LIVE=0`) + silent degradation (collector
exceptions swallowed, pipeline.py:112-114). Not a port-9222 problem (heavy sites use a fresh
headless pool, corp_audit.py:116-129). Fix = ruling + flag + supervisor visibility.

## Effect
- Blueprint delta: consensus.enabled false→true (weights_consensus.yaml:15); active pipeline
  gains `--live-consensus` (scrape_cmd.py); supervised watcher gains `-LiveConsensus` +
  `logs/consensus_status.json` + zero-evidence (S1/F1) notification.
- Success bars pre-registered (impl-plan B-20260818-001): S1 ≥2/5 sites with n≥1; S2 new
  run_ts, no SET-ASIDE-due-to-live-disabled; S3 supervisor sees consensus health; S4 gate PASS.
- Failure plan: F1 all-zero evidence → reject live-in-active-pass; F2 partial → whitelist;
  F4 supervisor can't observe → reject.
- Quality gate: consensus/discovery suites 23 passed; FULL suite 1182 passed / 18 skipped / 0 failures.

## Follow-up
- Run one supervised live consensus pass (`--live-consensus`), verify S1-S4.
- Record run_ts + cost row in cost-ledger.md.

## Run verdict (2026-08-18, pid 19352)
- **S1: FAIL** — zero evidence (0 usable, 0 reviews, NVDA SET-ASIDE, score 0.000); `logs/consensus_status.json` state=zero_evidence; watcher fired zero-evidence notification.
- **F1 triggered → REJECT live-in-active-pass; `consensus.enabled` reverted to false.**
- Cause analysis (NOT a Cloudflare wall across the board — infra/dependency failures):
  1. Port 9222 down → Glassdoor, Indeed-fallback, G2 session-warmup, LinkedIn all fail the attach (our fail-safe fired correctly).
  2. Indeed loads but "no rating extracted" (extraction mismatch).
  3. G2 direct fetch 403 ×2; Bing search proxied via 80.51.61.1 refused.
  4. Browserless localhost:3000 not running → Capterra.
  5. DNS: api.adzuna.com getaddrinfo failed; no house Trustpilot scraper.
- Positive: fail-closed design worked end-to-end — no guest-jar, no silent fake rows, supervisor observed the exact failure and alerted.
- Re-plan options for next ruling: launch debuggable browser (9222) before run; add `allow_fresh_fallback=True` for public review scrapers only (no cookie writes); run Browserless; verify DNS/proxy; fix Indeed extraction.

## Follow-up ruling (2026-08-18) — option B executed: debuggable browser launched
- Brave launched on 127.0.0.1:9222 with `config/browser_profiles/master` (visible, real session).
- nodriver attach verified OK; next `-LiveConsensus` loop (pid 5916, run_ts=1787102270) produced:
  **NVDA Score 0.203, Flags [], usable=2, reviews=4289 — S1 MET, no SET-ASIDE.**
- Remaining blockers (non-attach): Browserless localhost:3000 down (Capterra), Adzuna API 401,
  JobSpy not installed, Indeed extraction (if any). Supervisor status file now written at
  completion (full-log parse fix in run_supervised_pipeline.ps1).
- consensus.enabled remains false — the live pass runs via `--live-consensus` supervised without
  flipping the kill-switch; flip only on a further ruling.