POSITION-BY: big-pickle
BRIEF-ID: B-20260815-001
POSITION: APPROVE Hybrid A+B — build a weekly IG_LLM crawler/LLM-synthesis pipeline that programmatically constructs qualitative proxy scores (moat, sentiment, product reviews) as structured DB columns under the IG_LLM_ prefix, feeding the existing qualitative gate rather than bypassing it, then re-run rm-final as the go/no-go checkpoint.

REASONS:
1. Gate integrity preserved — proxy scores flow INTO the existing qualitative screen as additional evidence columns; the gate logic itself is never weakened or circumvented, which is the binding blueprint constraint.
2. IC signal unlocked — RFF256 (IC 0.054) is stranded behind the qual:hold wall; structured LLM proxies supply the missing moat/review/Glassdoor signals that the gate requires, converting a dead signal into a live candidate.
3. Provenance invariant satisfied — IG_LLM_ ticker prefix + DB-first persistence through `db/` layer gives every proxy score a traceable source, fulfilling blueprint invariant #2 (provenance everywhere) and invariant #1 (DB-first, WAL mode).

RISKS:
1. LLM hallucination in proxy scores — if the weekly synthesis pipeline injects fabricated moat or review signals, candidates pass the qual gate on false evidence; mitigation requires structured output schemas, source-URL grounding, and a human-readable audit trail per score.
2. Scope creep into gate logic — developers may be tempted to lower thresholds when proxies are sparse, which would de facto weaken the standard screen; the implementation brief must hard-prohibit any changes to gate thresholds or exclusion rules in the same PR.

BLIND-SPOTS:
- Did not verify whether the existing qualitative_scoring module has a defined extension point for additional proxy inputs or requires structural modification.
- Did not confirm crawler legality/TOS compliance for the target alternative sites (Glassdoor, Reddit proxies) under the current credentials config.
- Did not assess whether the weekly LLM synthesis latency is compatible with the existing APScheduler job cadence in scripts/scheduler.
