# D-20260823-001 — Stack A PIT Sandbox: Pipeline Skill Falsification

- **Tier:** 3
- **Date:** 2026-08-23
- **Brief:** B-20260823-001
- **Ruling:** APPROVE (conditional, verified)

## Options considered
1. Position A (big-pickle): Five-phase PIT sandbox with per-row `available_as_of` schema and quarantine partitions inside the db/ layer, using human-labeled corpora as the sole auto-grader, F1/Spearman against return labels in Phase 3.
2. Position B (gemini-planner): Strict "Time-Travel DB" sandbox wrapping SQLite with a simulated-clock timestamp parameter for all reads, evaluating pipeline skill via cross-entropy loss against verified human corpora first, deferring return-linked simulation until NLP skill is proven.

## Positions
- **big-pickle:** Five-phase PIT sandbox using per-row `available_as_of` schema and quarantine partitions, human-labeled corpora grading oracle, and pre-registered bars in `config/weights*.yaml`.
- **gemini-planner:** Strict "Time-Travel DB" wrapping SQLite with simulated-clock read wrapper and NLP-first evaluation sequence before market return simulation.

## Disagreement map (if T3)
- Disagree on:
  1. PIT enforcement mechanism: A = per-row `available_as_of` column + quarantine partitions inside the db/ layer; B = read-only "simulated-clock" wrapper over SQLite requiring every query to carry a clock parameter.
  2. Primary skill metric & sequence: A = F1/Spearman against return labels in Phase 3; B = cross-entropy vs verified human corpora first, deferring return-linked simulation until NLP skill is proven.
  3. First-run scope: A builds all five ablation columns up front; B stages NLP-only before any market simulation.
- Consensus on: human-labeled oracle replaces manual grading entirely; frozen hash-locked grading scripts; per-source isolation matrix; pre-registered bars; conductor gate per phase; tuning unlocked only after verdicts recorded.

## Synthesis (hermes-bridge, if T3)
- Recommendation: hybrid — A's schema (per-row `available_as_of` + quarantine partitions) + B's metric sequencing (NLP corpus skill proven first, return-linkage deferred to Phase 3+)
- Disagreements:
  - Substantive 1 — PIT mechanism: A's schema column physically quarantines contaminated rows; B's clock-wrapper is schema-free but relies on every query carrying the right clock parameter. A wins on auditability; sim-guardian can hash-verify the partition, not a wrapper call.
  - Substantive 2 — Metric staging: B's own RISK 2 names the flaw in A's sequencing — F1/Spearman against return labels in Phase 3 is a false-positive risk if corpus NLP skill was never isolated first. B's stage-order is more defensible scientifically.
- Rationale: A's per-row quarantine is the only design sim-guardian can provably audit via SHA-256 manifest; B's wrapper introduces an unverifiable runtime invariant. However, B correctly identifies that linking NLP skill to market returns before corpus accuracy is proven produces an unfalsifiable Phase 3 verdict. The hybrid takes A's storage layer and B's evaluation sequence, eliminating both failure modes.
- Risk: Financial PhraseBank license is unverified (A's blind-spot); if restricted, the grading oracle loses its primary corpus.

## CEO ruling
> CONDITIONAL APPROVE of the hybrid; condition (sources sortable by real dates) VERIFIED same day: transcripts dataset `date` field populated 2005-2025, MIT license; XBRL filed-dated; Damodaran year-labeled; Chen-Zimmermann vintaged; GDELT event-dated; PhraseBank/SemEval undated → atemporal oracle use only.

**Rationale:** The hybrid integrates Position A's per-row `available_as_of` schema and quarantine partitions (which sim-guardian can verify via SHA-256 manifest) with Position B's staged evaluation (proving NLP corpus skill before returning linkage). To prevent any runtime leakage, the system also incorporates sim-guardian's frozen-hash replay audit and a centralized clock-stepped read layer.

## Effect
- Blueprint delta: None (blueprint updated by custodian; no direct edit by logger).
- Quality gate: PASS — Full suite 1267 passed / 18 skipped / 0 failed (100% pass rate). 9 gates executed on feature/b-20260823-001: DA-1 timestamp coverage ≥80%, DA-2/B-2 oracle transfer >15pp drop fail, B-1 provenance rule-based-or-cutoff≤2009, SG-1 hash replay bit-identity + negative control.
- Cost rows: D-20260823-001 → cost-ledger.md

## Follow-up
- Await CEO commit approval for Phase 0 files on feature/b-20260823-001: `db/schema_pit.py`, `db/pit_reader.py`, `scripts/pit_phase0_audit.py`, `config/weights_sentinel_bars.yaml` (bars frozen pre-run), `tests/test_pit_sandbox.py`.
- Archive `_drafts/` artifacts under `_drafts/archive/D-20260823-001/`.
