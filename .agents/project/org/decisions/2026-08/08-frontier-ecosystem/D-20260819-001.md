# D-20260819-001 — Virus-Frontier: Index-Anchored Overlap Frontier (B-20260819-001)

- **Tier:** 3 (council hearing; DISCOVERY origin)
- **Date:** 2026-08-19
- **Brief:** B-20260819-001
- **Ruling:** APPROVE (hybrid, CEO-modified) — executed 2026-08-19, commit `61147ea`

## Options considered
1. **Option A (big-pickle):** Multi-vector reverse-lookup + hub-priority BFS — SEC Reg S-K 601(b)(21) >10% customer disclosures (via sec_lane companyfacts) as the authoritative, point-in-time, provenance-stamped backbone; SIC co-class + shared >5% 13F holder overlap as secondary EDGAR vectors; IG/thematic suppressed until the noise floor drops; stop a branch when no new authoritative SEC edge AND ecosystem.py chain_score < 0.3.
2. **Option B (gemini-planner):** Hybrid SIR-spread with authority-decay — R0 decays by hop-distance and edge quality; Direct Contact P=1.0 (SEC disclosures), Airborne P=0.5 (ecosystem.py co-mentions via Jina); QUARANTINE IG; hubs need multiple incoming edges to transmit; stop when R0 < baseline + hard 3-degree cap; nodes clear G1–G4 + CIK before infection.
3. **Option C (implicit):** Thematic/narrative co-mention spread (hashtag/text discovery) — rejected by council (IG noise floor: 315 "tickers" in 10 days, NVDA only 9 real rows, LINK/BIO/HERE pollution).
4. **Option D (CEO overlap-grading):** Index-anchored overlap frontier — grade suppliers by SHARED-CUSTOMER OVERLAP across the major set (not seed-only); expand upstream ("the spot before the shovel"); AI-cons vector for hidden gems; slow, ban-safe pacing. **ADOPTED as the final algorithm.**

## Positions
- **big-pickle (Position A):** SEC >10% disclosures as auditable backbone; hub-priority BFS; secondary 13F/SIC vectors from EDGAR only; IG suppressed until noise floor fixed.
- **gemini-planner (Position B):** SIR epidemiological spread with R0 decay + multi-edge infection threshold; quarantine IG; hard 3-degree cap; G1–G4 + CIK before infection.

## Disagreement map
- **Disagree on:** spread model (hub-BFS vs R0-decay); co-mention vector (suppress vs P=0.5 airborne); stopping rule (open R-frontier vs 3-degree hard cap); BFS depth (indefinite vs capped).
- **Consensus on:** IG quarantine (both agree); SEC disclosures as the only authoritative edge source; point-in-time provenance mandatory.

## Council voices (advisory)
- **devil-advocate:** A survives better (regulated, auditable edges) but A's own R<1 frontier contradicts virus expansion; neither position is investment-grade without stopping-rule verification + false-edge filtering.
- **open-minded (vote: B-modified, confidence 0.45):** SEC edges may be pure lag (already priced); both positions miss that the graph is TEMPORAL — alpha lives in edge appearance/disappearance, not static topology (Phase-2).
- **sim-guardian:** edges must be point-in-time; private-node truncation makes the R0<1 boundary self-confirming; common-word ticker blacklist required.
- **risk-automator:** hard caps ≤15%/node, cluster ρ>0.70 → 40% equity, ≤200 nodes/seed, ≤50 edges/node, $5/day; edges valid only CIK + ≤90d + ≥2 sources; FLAGS the idea as capital risk if edges encode only priced-in info or lack distance-decay.
- **execution-strategist:** SEC lane (0.5 rps) is the bottleneck; >3 degrees operationally unsafe; firing order SEC→GitHub→Reddit→Enrich→StockTwits→IG(last); IG = hygiene fallback only, off-market-hours.
- **alpha-integrator:** direct edges fully priced; alpha = two-hop shared-supplier + co-mention burst (5–15d windows, decay 3–5d); only ~4–6 independent bets; capacity $200–300k.

## Synthesis (hermes-bridge, S-20260819-001)
- **Recommendation:** Hybrid — SEC-primary authoritative backbone (A) + R0-decay stopping rule + hard 3-degree cap (B) + co-mention SUPPRESSED at launch + B's multi-edge infection threshold as false-edge brake.
- **Disagreements:** substantive (spread model, co-mention vector, stopping rule) vs stylistic (IG quarantine — non-issue).
- **Rationale:** SEC edges are the only auditable, provenance-stamped signal; co-mention and 13F vectors fail the ≥2-source gate at launch; two-hop shared-supplier paths (the live alpha surface) live inside 3 degrees.
- **Risk:** if hop-1 SEC edges encode only priced-in relationships, the graph yields zero edge; alpha-integrator's $200–300k capacity ceiling must be enforced before any position sizing.

## CEO ruling
> "Openminded does prove a point in which It would essentially a big index funds, however i believe a further search using a virus tactic will be able to help us find hidden gems." … "But I'm also in search of multiple common suppliers. For example if google, meta, NVDA, Intell all take TSMC. Then it should be important." … "Like NVDA is already the shovel, but find the spot before the shovel so they can tell what they sell." … "The priority should increase by the amount of overlap and graded."

> "I mean it needs attention but that's not necessary if it's a steal. it should be slowly progressive we can use an index fund such as qqq figure out it's weight and dynamically search." … "look for companies that attempt to solve the solution that AI is planning to create… look at the benefit and the cons… companies attempting to addressing the cons that following the quantitative gate." … "The seed should be set with NVDA, marks it's companies and then slowly compare across all major companies." … "For the weights it doesn't need to be NVDA specific it just needs to overlap with the similar companies."

> "Well we can use ETFs as a way, but in general use competitor of the company in question and look." … "Keep the multiseed but ensure we have very good… I want to take a few hours i don't need the data instantly I don't want to get banned." — "execute the build" — "commit"

**Rationale:** The CEO adopts the hybrid but upgrades the priority function to **shared-supplier overlap grading across the major set** (not seed-only), adds **upstream inheritance** ("the spot before the shovel" — tier-2 suppliers graded by who they serve), adds the **AI-cons vector** (companies solving AI's known problems, by sector + quant gate, narrative never required), uses **ETFs only as supplemental grading weights** (competitors-primary), demotes **IG to off-hours hygiene fallback**, and mandates **slow, ban-safe binge-block pacing**. The quant gate is the sole value filter ("a steal doesn't need attention") — the answer to open-minded's index-fund trap.

## Effect
- **Blueprint delta:** New discovery pillar — `discovery/frontier.py` (overlap-graded expansion), `discovery/ai_cons.py` (10-con taxonomy + screener + hidden-gem steal filter), `discovery/etf_weights.py` (QQQ weight cache), `ecosystem_graph` schema (point-in-time edges), `frontier` sentinel lane + `frontier-expand` CLI, IG demoted in run-all (`--with-ig`, off-hours). Ruling block appended to blueprint.md.
- **Quality gate:** PASS — full suite 1236 passed / 18 skipped / 0 failed (conductor ≥90%). 51 new tests (frontier grading, upstream inheritance, pacing, AI-cons screen, ETF cache).
- **Cost rows:** D-20260819-001 → cost-ledger.md

## Follow-up
- Live SEC >10% customer extraction (companyfacts) to feed edge maps — currently the engine runs on pre-registered `config/frontier_edges.yaml` seed + persisted edges.
- ETF weight source live fetcher (local CSV) — needs the QQQ holdings file to populate the denominator.
- Phase-3 temporal edges (open-minded): track edge birth/death per quarter as a leading signal.
- Phase-4 frontend web-of-interconnection from `ecosystem_graph` (edge thickness = overlap grade).
- Risk caps (≤15%/node, ≤200 nodes/seed, ≤50 edges/node, $5/day) enforced before any live position sizing.

## Drafts

Before a ruling, briefs/positions/synthesis accumulate in `_drafts/` under the brief-id. After a ruling, archive them at `_drafts/archive/<DECISION-ID>/`.