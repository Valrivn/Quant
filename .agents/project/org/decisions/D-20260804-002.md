# D-20260804-002 — 7-Phase PIT Data-Layer Rebuild (Hybrid)

- **Tier:** 3 | 2 | DISCOVERY
- **Date:** 2026-08-04
- **Brief:** B-20260804-002
- **Ruling:** APPROVE hybrid

## Options considered
1. Full 7-phase PIT rebuild (Position A, big-pickle)
2. Minimal 3-phase patch on #1/#4/#3 (Position B, gemini-planner)

## Positions
- **big-pickle:** Full 7-phase data-layer-only build (P1-P7), pre-registered S1-S6, then Discovery re-run reporting % deltas vs degraded run with zero strategy-logic changes
- **gemini-planner:** Minimal 3-phase patch on #1/#4/#3 (FRED, basket-empty, cross-check wiring) with DEGRADED fallback for missing APIs

## Disagreement map (if T3)
- Disagree on: Scope (7-phase vs 3-phase), S1 falsifiability, external-dependency risk, survivorship/universe coverage, decision-delay impact
- Consensus on: #1 (FRED) and #4 (basket-empty) are materially-moving degradations; #3 (cross-check wiring) must be fixed; data-layer only; zero strategy-logic changes; re-run reports deltas honestly; external key availability is shared risk

## Synthesis (hermes-bridge, if T3)
- Recommendation: hybrid — execute Position A's full 7-phase rebuild, but gate Phase P2+ on a pre-build API probe (FRED/Tiingo/EDGAR keys + quotas verified on-disk within 1 hour); if any key is blocked, fall back to Position B's 3-phase patch for that source only, with a DEGRADED ledger entry, and proceed — never halt the whole build for one missing API
- Disagreements: substantive vs stylistic (S1 falsifiability framing)
- Rationale: The CEO's stated requirement is full backtest accuracy and all six success criteria are pre-registered — Position B's patch explicitly leaves survivorship bias and look-ahead intact, which fails S1/S5 and makes the Discovery delta report unreliable. Position A is the only path that honestly answers whether the buy-more thesis survives corrected data. The API-probe gate converts Position B's blocking risk into a scoped fallback rather than a veto
- Risk: API key failure on FRED or Tiingo blocks P2 and delays the full rebuild by 1–3 days; DEGRADED-ledger fallback path must be pre-wired before build starts

## CEO ruling
> APPROVE hybrid — full 7-phase PIT data-layer rebuild gated on <=1-hour API probe before P2, per-source DEGRADED fallback (never full stop), then Discovery re-run with % deltas and re-evaluated success bars

**Rationale:** CEO requires full backtest accuracy, S1-S6 pre-registered, Position A is the only honest path, probe gate converts B's blocker risk into scoped fallback

## Effect
- Blueprint delta: Data-layer rebuild scope expanded to include full PIT accuracy with API-probe gating and per-source DEGRADED fallback
- Quality gate: PASS/FAIL + pass rate (to be determined after build)
- Cost rows: D-20260804-002 → cost-ledger.md

## Follow-up
- Implementation plan: research/backtest-accuracy-plan.md (embeds full debate record + ruling)
- Archive drafts: _drafts/archive/D-20260804-002/
- Success criteria: S1-S6 from brief B-20260804-002 must be met
- No code merged yet — the ruling authorizes the build; the 7 phases are the delivery contract