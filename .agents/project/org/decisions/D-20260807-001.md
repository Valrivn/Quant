# D-20260807-001 — Instagram Independent Discovery Experiment (alpha-gate test)

- **Tier:** 3
- **Date:** 2026-08-07
- **Brief:** B-20260807-001
- **Ruling:** MODIFY

## Options considered
1. big-pickle (Position A): keep IG/TikTok as corroborating peer gated on structured sources; fill Reddit/StockTwits/ApeWisdom credentials to unblock the gate.
2. gemini-planner (Position B): decouple IG/TikTok as an independent channel now to inject non-consensus alpha.
3. hermes hybrid: fill credentials and re-run census first; only unlock IG if pass-through stays 0% after live creds.

## Positions
- big-pickle A: gate IG on structured evidence; credential gap is the census blocker.
- gemini-planner B: IG should be independent and decoupled; ranker already supports per-source weights.
- hermes-bridge S: hybrid — fill creds first, re-run census, decouple IG only if pass-through stays 0%.

## CEO ruling
> MODIFY — StockTwits/ApeWisdom credential pipeline is NOT the path: the Reddit scraper already runs via browser driver (UnifiedScraperSession on `SUBREDDIT_TAXONOMY`, no API keys). Treat Instagram/TikTok as an INDEPENDENT experimental discovery channel (not gated on Reddit). Its candidate mentions go through the STANDARD stock screen — qualitative engine + quantitative baseline (the census gates) — to evaluate whether IG uncovers alpha. Compare the IG-screened companies against the companies our existing scrapers already surface. Isolation contract stands: `discovery/` remains a leaf; `discovery.enabled: false`; research-only; RM-FINAL untouched; no merge without a gate pass.

## Effect
- Blueprint delta: IG/Tik = independent discovery experiment, standard full-screen qual+quant gate, alpha comparison vs scraper companies; Stocktwatters/Stock/Ape deprioritized on the census path.
- Isolation: DISCOVERY feed is leaf; kill-switch off; frozen paths untouched (hashes).
- Quality gate: PASS (pending implementation).
- Cost rows: D-20260807-001 → cost-ledger.md

## Follow-up
- Record in decisions/index, cost ledger, tendencies, blueprint.
- Build isolated IG alpha experiment (experimental corpus/feed, rediscovery/iod_candidates) reusing the census gates.
- Document verdict research artifact. No commit without CEO approval.